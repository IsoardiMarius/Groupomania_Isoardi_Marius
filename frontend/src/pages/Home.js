import React from 'react';
import Logo from "../components/Logo";

const Home = () => {
    return (
        <div >
            <Logo style={{marginTop:'50px'}}/>
        </div>
    );
};

export default Home;


 // token ? /home : /signin